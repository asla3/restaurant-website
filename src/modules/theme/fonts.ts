import { Roboto_Slab, Rubik } from 'next/font/google';

export const robotoSlab = Roboto_Slab({ subsets: ['latin'], display: 'swap' });

export const rubik = Rubik({ subsets: ['latin'], display: 'swap' });
